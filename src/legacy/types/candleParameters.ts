export type candleParameters = {
    params: {
        high?: number,
        open?: number,
        low?: number,
        close?: number,
        date?: string,
        home_team_img?: string,
        away_team_img?: string,
        score?: number[],
        home_team?: string,
        away_team?: string,
        time?: string
    }
}